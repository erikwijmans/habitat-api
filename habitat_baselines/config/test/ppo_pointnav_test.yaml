defaults:
- habitat_baselines/trainer: ppo
- habitat_baselines/env: nav_rl
- habitat_baselines/model: baseline
habitat:
  environment:
    max_episode_steps: 50
habitat_baselines:
  eval:
    ckpt_path_dir: data/test_checkpoints/ppo/pointnav/ckpt.0.pth
  logging:
    checkpoint_folder: data/test_checkpoints/ppo/pointnav/
    checkpoint_interval: 1
    interval: 100
  model:
    hidden_size: 128
  num_processes: 1
  simulator_gpu_id: 0
  tensorborad:
    dir: ""
  torch_gpu_id: 0
  trainer:
    ppo:
      num_mini_batch: 1
      num_steps: 32
      num_updates: 5
      ppo_epoch: 2
  video:
    outputs: []
